services:
  web:
    build: .
    image: tiisto/paris2026:latest
    container_name: paris2026-app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DATA_DIR=/data
    volumes:
      - paris_data:/data
    restart: unless-stopped

volumes:
  paris_data:
